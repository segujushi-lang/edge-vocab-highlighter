# 隐私说明

“拾词”只处理实现生词高亮所需的数据：

- 保存的单词、中文翻译、保存时间、来源页面地址、所属分类、分类名称和分类颜色存放在 Edge 的扩展本地存储中。
- 为支持撤回与重做，扩展会在浏览器会话存储中临时保留最多 20 个操作快照；关闭浏览器会话后，这些历史自动清除。
- 未手工填写翻译时，扩展只把所选英文单词发送到 MyMemory 翻译接口 `api.mymemory.translated.net`，用于获取中文翻译。
- 用户选择的 TXT 或 Markdown 文件只在扩展弹窗中读取和解析，原文件内容不会上传或保存。只有确认导入后的有效单词和已有中文释义会进入本地词库。
- 批量导入中未附中文释义的单词不会自动发送给任何服务；只有用户之后主动点击该词条的“重试翻译”时，该单个英文单词才会发送到 MyMemory。
- 来源页面地址不会发送给翻译服务。
- 扩展不会收集浏览历史、账号、表单内容或网页正文，也没有自建服务器。
- 删除单词或清空词库后，对应本地词条会被移除；在当前浏览器会话内执行撤回时，相关词条可能按用户指令恢复。
- 删除自定义分类时，其中的单词只会移回默认分类，不会随分类一起删除；分类操作也可以在当前会话内撤回。

MyMemory 服务本身的数据处理规则由其服务条款和隐私政策约束。若不希望发出翻译请求，可以在添加单词时手工填写中文翻译。
